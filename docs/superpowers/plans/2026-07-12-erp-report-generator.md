# ERP Report Generator — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a professional PHP 8.3 ERP report generator (Arabic RTL) with 3 report templates (Sales, Inventory, Finance) + dashboard, backed by SQLite, tested via Playwright, pushed incrementally to a Claude-created GitHub repo.

**Architecture:** Vanilla PHP with a minimal MVC front-controller. `public/index.php` routes to Controllers, which call Repositories (PDO/SQLite) and render PHP Views inside an RTL layout. Charts via Chart.js (CDN). Export is native PHP (CSV/`.xls`) plus browser print for PDF.

**Tech Stack:** PHP 8.3.28 (WampServer, run via `php -S`), SQLite (pdo_sqlite), Chart.js (CDN), Cairo font (CDN), Playwright MCP for E2E.

## Global Constraints

- PHP binary: `C:\wamp64\bin\php\php8.3.28\php.exe` (no global `php` on PATH — always use full path).
- Serve with: `"C:/wamp64/bin/php/php8.3.28/php.exe" -S 127.0.0.1:8010 -t public public/index.php` (the trailing `public/index.php` router script is REQUIRED — without it the built-in server returns 404 for `/assets/*` since assets live outside the docroot).
- No Composer, no frameworks, no server-side third-party libs. Front-end CDN (Chart.js, font) is allowed.
- All UI in Arabic, `dir="rtl"`, `lang="ar"`, UTF-8 everywhere.
- Currency display: Saudi Riyal, format `#,##0` + " ر.س".
- DB file `database/erp.sqlite` is git-ignored; it is regenerated by `database/seed.php`.
- Every task ends by committing; pushes to GitHub happen at the milestone boundaries noted in the spec (Task 1 sets up the remote).
- Repository/table/column names exactly as defined in Task 3 schema.

---

### Task 1: Project scaffold + GitHub repo creation

**Files:**
- Create: `public/index.php` (temporary smoke page, replaced in Task 2)
- Create: `README.md`
- Verify: `.gitignore` (already exists from spec commit)

**Interfaces:**
- Produces: a running `php -S` server root at `public/`, and a GitHub remote named `origin`.

- [ ] **Step 1: Install gh CLI and authenticate**

Run:
```bash
winget install --id GitHub.cli -e --accept-source-agreements --accept-package-agreements
```
Then in a NEW shell (so PATH refreshes):
```bash
gh auth login
```
Expected: interactive browser login completes; `gh auth status` shows the logged-in user.

If `winget` is unavailable, download the MSI from https://github.com/cli/cli/releases and install, then re-run `gh auth login`.

- [ ] **Step 2: Create a temporary smoke page**

Create `public/index.php`:
```php
<?php
header('Content-Type: text/plain; charset=utf-8');
echo "ERP Report Generator — up\n";
echo "PHP " . PHP_VERSION . "\n";
```

- [ ] **Step 3: Run the server and verify it responds**

Run (background):
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" -S 127.0.0.1:8010 -t public
```
In another shell:
```bash
curl -s http://127.0.0.1:8010/
```
Expected: `ERP Report Generator — up` and a PHP 8.3 line. Stop the server after.

- [ ] **Step 4: Write README**

Create `README.md`:
```markdown
# مولد تقارير ERP

مولد تقارير احترافي (PHP 8.3 + SQLite) بواجهات عربية RTL: المبيعات، المخزون، المالية + لوحة تحكم.

## التشغيل
```bash
# 1) توليد قاعدة البيانات والبيانات التجريبية
"C:/wamp64/bin/php/php8.3.28/php.exe" database/seed.php
# 2) تشغيل الخادم
"C:/wamp64/bin/php/php8.3.28/php.exe" -S 127.0.0.1:8010 -t public
# 3) افتح
http://127.0.0.1:8010/
```

## البنية
- `public/` جذر الخادم — `index.php` (Front Controller)
- `app/Core` — Router, Database, Controller
- `app/Controllers`, `app/Models`, `app/Views`
- `database/` — schema + seed
- `tests/e2e` — اختبارات Playwright
- `docs/` — الوثائق وتقرير المشروع
```

- [ ] **Step 5: Create the GitHub repo (Claude creates it) and push**

Run:
```bash
gh repo create erp-report-generator --public --source=. --remote=origin --description "مولد تقارير ERP احترافي بـ PHP + SQLite (عربي RTL)" --push
```
Expected: repo created under the authenticated user; `git remote -v` shows `origin`; initial history pushed.

- [ ] **Step 6: Commit any remaining changes**

```bash
git add -A && git commit -m "chore: scaffold project + smoke page + README" && git push
```
Expected: clean tree, pushed to origin. (If the create --push already committed, this is a no-op or a follow-up commit.)

---

### Task 2: Core — Router, Database, Controller, Request, front controller

**Files:**
- Create: `app/Core/Database.php`
- Create: `app/Core/Router.php`
- Create: `app/Core/Controller.php`
- Create: `app/Core/Request.php`
- Modify: `public/index.php` (replace smoke page with real front controller)
- Create: `app/Views/layout.php` (minimal, expanded in Task 5)
- Test: `tests/unit/router_test.php`

**Interfaces:**
- Produces:
  - `Database::connection(): PDO` — singleton PDO to `database/erp.sqlite`.
  - `class Router { add(string $path, callable|array $handler): void; dispatch(string $uri): void }` where handler `[ControllerClass::class, 'method']`.
  - `abstract class Controller { protected function render(string $view, array $data = []): void; protected function json(array $data): void }` — `render` includes `app/Views/layout.php` with `$data` extracted and `$view` as the content view name.
  - `class Request { static function get(string $key, ?string $default = null): ?string }` — reads `$_GET`, trims.

- [ ] **Step 1: Write the failing test**

Create `tests/unit/router_test.php`:
```php
<?php
require __DIR__ . '/../../app/Core/Router.php';

$r = new Router();
$hit = null;
$r->add('/ping', function () use (&$hit) { $hit = 'pong'; });
$r->dispatch('/ping');
assert($hit === 'pong', 'route /ping should invoke handler');

$r->add('/none', function () use (&$hit) { $hit = 'x'; });
$r->dispatch('/does-not-exist');
assert($hit === 'pong', 'unknown route should not invoke handlers (404)');

echo "router_test OK\n";
```

- [ ] **Step 2: Run test to verify it fails**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/router_test.php
```
Expected: FAIL — `Failed opening required '.../app/Core/Router.php'`.

- [ ] **Step 3: Implement Database.php**

Create `app/Core/Database.php`:
```php
<?php

class Database
{
    private static ?PDO $pdo = null;

    public static function connection(): PDO
    {
        if (self::$pdo === null) {
            $path = __DIR__ . '/../../database/erp.sqlite';
            self::$pdo = new PDO('sqlite:' . $path);
            self::$pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
            self::$pdo->setAttribute(PDO::ATTR_DEFAULT_FETCH_MODE, PDO::FETCH_ASSOC);
            self::$pdo->exec('PRAGMA foreign_keys = ON');
        }
        return self::$pdo;
    }
}
```

- [ ] **Step 4: Implement Router.php**

Create `app/Core/Router.php`:
```php
<?php

class Router
{
    private array $routes = [];

    public function add(string $path, callable|array $handler): void
    {
        $this->routes[$path] = $handler;
    }

    public function dispatch(string $uri): void
    {
        $path = parse_url($uri, PHP_URL_PATH) ?: '/';
        $path = rtrim($path, '/') ?: '/';
        if (!isset($this->routes[$path])) {
            http_response_code(404);
            echo 'صفحة غير موجودة (404)';
            return;
        }
        $handler = $this->routes[$path];
        if (is_array($handler)) {
            [$class, $method] = $handler;
            (new $class())->$method();
        } else {
            $handler();
        }
    }
}
```

- [ ] **Step 5: Implement Request.php**

Create `app/Core/Request.php`:
```php
<?php

class Request
{
    public static function get(string $key, ?string $default = null): ?string
    {
        if (!isset($_GET[$key]) || $_GET[$key] === '') {
            return $default;
        }
        return trim((string) $_GET[$key]);
    }
}
```

- [ ] **Step 6: Implement Controller.php**

Create `app/Core/Controller.php`:
```php
<?php

abstract class Controller
{
    protected function render(string $view, array $data = []): void
    {
        $data['__view'] = __DIR__ . '/../Views/' . $view . '.php';
        extract($data, EXTR_SKIP);
        require __DIR__ . '/../Views/layout.php';
    }

    protected function json(array $data): void
    {
        header('Content-Type: application/json; charset=utf-8');
        echo json_encode($data, JSON_UNESCAPED_UNICODE);
    }
}
```

- [ ] **Step 7: Create a minimal layout**

Create `app/Views/layout.php`:
```php
<?php /** @var string $__view */ ?>
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><?= htmlspecialchars($title ?? 'تقارير ERP') ?></title>
</head>
<body>
<main>
    <?php require $__view; ?>
</main>
</body>
</html>
```

- [ ] **Step 8: Wire the front controller**

Replace `public/index.php`:
```php
<?php
declare(strict_types=1);

spl_autoload_register(function (string $class) {
    foreach (['Core', 'Controllers', 'Models'] as $dir) {
        $file = __DIR__ . '/../app/' . $dir . '/' . $class . '.php';
        if (is_file($file)) { require $file; return; }
    }
});

require __DIR__ . '/../app/Core/Router.php';
require __DIR__ . '/../app/Core/Controller.php';
require __DIR__ . '/../app/Core/Database.php';
require __DIR__ . '/../app/Core/Request.php';

$router = new Router();
$router->add('/', [DashboardController::class, 'index']);
$router->add('/sales', [SalesController::class, 'index']);
$router->add('/inventory', [InventoryController::class, 'index']);
$router->add('/finance', [FinanceController::class, 'index']);
$router->add('/export', [ExportController::class, 'index']);

$router->dispatch($_SERVER['REQUEST_URI'] ?? '/');
```

- [ ] **Step 9: Run the router test — verify it passes**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/router_test.php
```
Expected: `router_test OK`.

- [ ] **Step 10: Commit and push**

```bash
git add -A && git commit -m "feat(core): router, database, controller, request, front controller" && git push
```

---

### Task 3: Database schema + seed data

**Files:**
- Create: `database/schema.sql`
- Create: `database/seed.php`
- Test: `tests/unit/seed_test.php`

**Interfaces:**
- Produces: `database/erp.sqlite` with tables `products`, `sales_invoices`, `sales_items`, `transactions`, populated with deterministic sample data (no randomness that breaks re-runs — use seeded pseudo-random via a fixed sequence).
- Column contract (relied on by all Repositories in Tasks 4–7):
  - `products(id, name, category, price, cost, stock_qty, reorder_level)`
  - `sales_invoices(id, invoice_no, customer_name, invoice_date, total)`
  - `sales_items(id, invoice_id, product_id, qty, unit_price, line_total)`
  - `transactions(id, type, category, amount, txn_date, description)` where `type IN ('income','expense')`.

- [ ] **Step 1: Write the failing test**

Create `tests/unit/seed_test.php`:
```php
<?php
require __DIR__ . '/../../app/Core/Database.php';

$db = Database::connection();
$counts = [
    'products' => (int) $db->query('SELECT COUNT(*) FROM products')->fetchColumn(),
    'sales_invoices' => (int) $db->query('SELECT COUNT(*) FROM sales_invoices')->fetchColumn(),
    'sales_items' => (int) $db->query('SELECT COUNT(*) FROM sales_items')->fetchColumn(),
    'transactions' => (int) $db->query('SELECT COUNT(*) FROM transactions')->fetchColumn(),
];
assert($counts['products'] >= 25, 'expected >=25 products, got ' . $counts['products']);
assert($counts['sales_invoices'] >= 150, 'expected >=150 invoices, got ' . $counts['sales_invoices']);
assert($counts['sales_items'] >= 150, 'expected >=150 sale items');
assert($counts['transactions'] >= 100, 'expected >=100 transactions');

// invoice total equals sum of its items (data integrity)
$bad = (int) $db->query(
    'SELECT COUNT(*) FROM sales_invoices i
     WHERE ROUND(i.total,2) <> ROUND(
       (SELECT COALESCE(SUM(line_total),0) FROM sales_items WHERE invoice_id = i.id), 2)'
)->fetchColumn();
assert($bad === 0, "invoice totals must equal sum of items; mismatches: $bad");

echo "seed_test OK\n";
```

- [ ] **Step 2: Run test to verify it fails**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/seed_test.php
```
Expected: FAIL — no such table `products` (or missing DB).

- [ ] **Step 3: Write schema.sql**

Create `database/schema.sql`:
```sql
DROP TABLE IF EXISTS sales_items;
DROP TABLE IF EXISTS sales_invoices;
DROP TABLE IF EXISTS transactions;
DROP TABLE IF EXISTS products;

CREATE TABLE products (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    category TEXT NOT NULL,
    price REAL NOT NULL,
    cost REAL NOT NULL,
    stock_qty INTEGER NOT NULL,
    reorder_level INTEGER NOT NULL
);

CREATE TABLE sales_invoices (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    invoice_no TEXT NOT NULL,
    customer_name TEXT NOT NULL,
    invoice_date TEXT NOT NULL,
    total REAL NOT NULL
);

CREATE TABLE sales_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    invoice_id INTEGER NOT NULL REFERENCES sales_invoices(id),
    product_id INTEGER NOT NULL REFERENCES products(id),
    qty INTEGER NOT NULL,
    unit_price REAL NOT NULL,
    line_total REAL NOT NULL
);

CREATE TABLE transactions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    type TEXT NOT NULL CHECK (type IN ('income','expense')),
    category TEXT NOT NULL,
    amount REAL NOT NULL,
    txn_date TEXT NOT NULL,
    description TEXT NOT NULL
);
```

- [ ] **Step 4: Write seed.php**

Create `database/seed.php`. Uses a deterministic LCG so re-runs are stable (no `rand()`), covers the 12 months ending 2026-06 (relative to spec date 2026-07-12).
```php
<?php
require __DIR__ . '/../app/Core/Database.php';

$dbPath = __DIR__ . '/erp.sqlite';
if (is_file($dbPath)) { unlink($dbPath); }

$db = Database::connection();
$db->exec(file_get_contents(__DIR__ . '/schema.sql'));

// Deterministic pseudo-random (LCG) — stable across runs.
$state = 12345;
$rand = function (int $min, int $max) use (&$state): int {
    $state = ($state * 1103515245 + 12345) & 0x7fffffff;
    return $min + ($state % ($max - $min + 1));
};

$categories = ['إلكترونيات', 'أثاث مكتبي', 'قرطاسية', 'أجهزة', 'ملحقات'];
$productNames = [
    'حاسوب محمول', 'شاشة عرض', 'لوحة مفاتيح', 'فأرة', 'طابعة', 'ماسح ضوئي',
    'كرسي مكتب', 'مكتب خشبي', 'خزانة ملفات', 'رف كتب', 'دفتر ملاحظات', 'أقلام حبر',
    'ورق طباعة', 'حبر طابعة', 'كاميرا ويب', 'سماعة رأس', 'ميكروفون', 'موزع USB',
    'قرص صلب', 'ذاكرة تخزين', 'كابل شبكة', 'راوتر', 'جهاز عرض', 'سبورة',
    'آلة حاسبة', 'مكبر صوت', 'شاحن', 'حامل هاتف', 'مقص', 'دباسة',
];

// products
$pStmt = $db->prepare('INSERT INTO products (name, category, price, cost, stock_qty, reorder_level) VALUES (?,?,?,?,?,?)');
$productIds = [];
foreach ($productNames as $i => $name) {
    $cost = $rand(50, 3000);
    $price = (int) round($cost * (1.2 + $rand(0, 40) / 100)); // 20%–60% markup
    $stock = $rand(0, 120);
    $reorder = $rand(10, 30);
    $pStmt->execute([$name, $categories[$i % count($categories)], $price, $cost, $stock, $reorder]);
    $productIds[] = (int) $db->lastInsertId();
}

// sales invoices + items across 12 months (2025-07 .. 2026-06)
$customers = ['مؤسسة النور', 'شركة الأفق', 'مكتب الرياض', 'مجموعة الخليج', 'مؤسسة البناء',
    'شركة المستقبل', 'متجر السلام', 'مؤسسة الرواد', 'شركة الإبداع', 'مكتب الإنجاز'];
$invStmt = $db->prepare('INSERT INTO sales_invoices (invoice_no, customer_name, invoice_date, total) VALUES (?,?,?,?)');
$itemStmt = $db->prepare('INSERT INTO sales_items (invoice_id, product_id, qty, unit_price, line_total) VALUES (?,?,?,?,?)');

$prices = [];
foreach ($db->query('SELECT id, price FROM products') as $row) { $prices[(int)$row['id']] = (float)$row['price']; }

$invoiceNo = 1000;
for ($m = 0; $m < 12; $m++) {
    $year = 2025 + intdiv(6 + $m, 12);
    $month = ((6 + $m) % 12) + 1; // 7..12 then 1..6
    $count = $rand(12, 22); // invoices per month
    for ($k = 0; $k < $count; $k++) {
        $day = str_pad((string) $rand(1, 28), 2, '0', STR_PAD_LEFT);
        $mm = str_pad((string) $month, 2, '0', STR_PAD_LEFT);
        $date = "$year-$mm-$day";
        $invStmt->execute(["INV-" . (++$invoiceNo), $customers[$rand(0, count($customers)-1)], $date, 0]);
        $invId = (int) $db->lastInsertId();
        $lines = $rand(1, 5);
        $total = 0.0;
        for ($l = 0; $l < $lines; $l++) {
            $pid = $productIds[$rand(0, count($productIds)-1)];
            $qty = $rand(1, 8);
            $unit = $prices[$pid];
            $lineTotal = $qty * $unit;
            $itemStmt->execute([$invId, $pid, $qty, $unit, $lineTotal]);
            $total += $lineTotal;
        }
        $db->prepare('UPDATE sales_invoices SET total = ? WHERE id = ?')->execute([$total, $invId]);
    }
}

// transactions: income (from sales-ish) + expenses across same 12 months
$expenseCats = ['رواتب', 'إيجار', 'كهرباء', 'تسويق', 'صيانة', 'مشتريات'];
$txnStmt = $db->prepare('INSERT INTO transactions (type, category, amount, txn_date, description) VALUES (?,?,?,?,?)');
for ($m = 0; $m < 12; $m++) {
    $year = 2025 + intdiv(6 + $m, 12);
    $month = ((6 + $m) % 12) + 1;
    $mm = str_pad((string) $month, 2, '0', STR_PAD_LEFT);
    // monthly income lump(s)
    for ($i = 0; $i < $rand(3, 6); $i++) {
        $day = str_pad((string) $rand(1, 28), 2, '0', STR_PAD_LEFT);
        $txnStmt->execute(['income', 'مبيعات', $rand(5000, 40000), "$year-$mm-$day", 'إيراد مبيعات']);
    }
    // monthly expenses
    for ($i = 0; $i < $rand(4, 8); $i++) {
        $day = str_pad((string) $rand(1, 28), 2, '0', STR_PAD_LEFT);
        $cat = $expenseCats[$rand(0, count($expenseCats)-1)];
        $txnStmt->execute(['expense', $cat, $rand(1000, 25000), "$year-$mm-$day", 'مصروف ' . $cat]);
    }
}

echo "Seed complete.\n";
```

- [ ] **Step 5: Generate the database**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" database/seed.php
```
Expected: `Seed complete.` and `database/erp.sqlite` created.

- [ ] **Step 6: Run the seed test — verify it passes**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/seed_test.php
```
Expected: `seed_test OK`.

- [ ] **Step 7: Commit and push**

```bash
git add -A && git commit -m "feat(db): schema + deterministic seed data" && git push
```

---

### Task 4: Sales report (repository + controller + view)

**Files:**
- Create: `app/Models/SalesRepository.php`
- Create: `app/Controllers/SalesController.php`
- Create: `app/Views/sales.php`
- Test: `tests/unit/sales_repo_test.php`

**Interfaces:**
- Consumes: `Database::connection()`, `Request::get()`.
- Produces:
  - `SalesRepository::summary(?string $from, ?string $to): array` → `['total'=>float,'count'=>int,'avg'=>float]`.
  - `SalesRepository::monthly(?string $from, ?string $to): array` → list of `['ym'=>'YYYY-MM','total'=>float]` ordered by ym.
  - `SalesRepository::topProducts(?string $from, ?string $to, int $limit=10): array` → list of `['name'=>string,'qty'=>int,'revenue'=>float]`.
  - `SalesRepository::invoices(?string $from, ?string $to, int $limit=50): array` → list of invoice rows.

- [ ] **Step 1: Write the failing test**

Create `tests/unit/sales_repo_test.php`:
```php
<?php
require __DIR__ . '/../../app/Core/Database.php';
require __DIR__ . '/../../app/Models/SalesRepository.php';

$repo = new SalesRepository();
$s = $repo->summary(null, null);
assert($s['count'] > 0, 'summary count > 0');
assert($s['total'] > 0, 'summary total > 0');
assert(abs($s['avg'] - ($s['total'] / $s['count'])) < 0.01, 'avg = total/count');

$monthly = $repo->monthly(null, null);
assert(count($monthly) > 0, 'monthly not empty');
assert(isset($monthly[0]['ym'], $monthly[0]['total']), 'monthly row shape');

$top = $repo->topProducts(null, null, 5);
assert(count($top) > 0 && count($top) <= 5, 'topProducts respects limit');
assert(isset($top[0]['name'], $top[0]['qty'], $top[0]['revenue']), 'top row shape');

$inv = $repo->invoices(null, null, 10);
assert(count($inv) > 0 && count($inv) <= 10, 'invoices respects limit');

// date filter narrows results
$all = $repo->summary(null, null)['count'];
$narrow = $repo->summary('2026-01-01', '2026-06-30')['count'];
assert($narrow <= $all, 'filtered count <= all');

echo "sales_repo_test OK\n";
```

- [ ] **Step 2: Run test to verify it fails**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/sales_repo_test.php
```
Expected: FAIL — cannot open `SalesRepository.php`.

- [ ] **Step 3: Implement SalesRepository.php**

Create `app/Models/SalesRepository.php`:
```php
<?php

class SalesRepository
{
    private PDO $db;
    public function __construct() { $this->db = Database::connection(); }

    private function range(?string $from, ?string $to): array
    {
        $where = [];
        $params = [];
        if ($from) { $where[] = 'invoice_date >= ?'; $params[] = $from; }
        if ($to)   { $where[] = 'invoice_date <= ?'; $params[] = $to; }
        $sql = $where ? (' WHERE ' . implode(' AND ', $where)) : '';
        return [$sql, $params];
    }

    public function summary(?string $from, ?string $to): array
    {
        [$w, $p] = $this->range($from, $to);
        $row = $this->db->prepare("SELECT COUNT(*) c, COALESCE(SUM(total),0) t FROM sales_invoices$w");
        $row->execute($p);
        $r = $row->fetch();
        $count = (int) $r['c'];
        $total = (float) $r['t'];
        return ['total' => $total, 'count' => $count, 'avg' => $count ? $total / $count : 0.0];
    }

    public function monthly(?string $from, ?string $to): array
    {
        [$w, $p] = $this->range($from, $to);
        $stmt = $this->db->prepare(
            "SELECT substr(invoice_date,1,7) ym, SUM(total) total
             FROM sales_invoices$w GROUP BY ym ORDER BY ym"
        );
        $stmt->execute($p);
        return array_map(fn($r) => ['ym' => $r['ym'], 'total' => (float) $r['total']], $stmt->fetchAll());
    }

    public function topProducts(?string $from, ?string $to, int $limit = 10): array
    {
        $where = [];
        $params = [];
        if ($from) { $where[] = 'i.invoice_date >= ?'; $params[] = $from; }
        if ($to)   { $where[] = 'i.invoice_date <= ?'; $params[] = $to; }
        $w = $where ? (' WHERE ' . implode(' AND ', $where)) : '';
        $params[] = $limit;
        $stmt = $this->db->prepare(
            "SELECT p.name name, SUM(si.qty) qty, SUM(si.line_total) revenue
             FROM sales_items si
             JOIN sales_invoices i ON i.id = si.invoice_id
             JOIN products p ON p.id = si.product_id$w
             GROUP BY p.id ORDER BY revenue DESC LIMIT ?"
        );
        $stmt->execute($params);
        return array_map(fn($r) => [
            'name' => $r['name'], 'qty' => (int) $r['qty'], 'revenue' => (float) $r['revenue']
        ], $stmt->fetchAll());
    }

    public function invoices(?string $from, ?string $to, int $limit = 50): array
    {
        [$w, $p] = $this->range($from, $to);
        $p[] = $limit;
        $stmt = $this->db->prepare(
            "SELECT invoice_no, customer_name, invoice_date, total
             FROM sales_invoices$w ORDER BY invoice_date DESC, id DESC LIMIT ?"
        );
        $stmt->execute($p);
        return $stmt->fetchAll();
    }
}
```

- [ ] **Step 4: Run the repo test — verify it passes**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/sales_repo_test.php
```
Expected: `sales_repo_test OK`.

- [ ] **Step 5: Implement SalesController.php**

Create `app/Controllers/SalesController.php`:
```php
<?php

class SalesController extends Controller
{
    public function index(): void
    {
        $repo = new SalesRepository();
        $from = Request::get('from');
        $to = Request::get('to');
        $this->render('sales', [
            'title' => 'تقرير المبيعات',
            'active' => 'sales',
            'from' => $from,
            'to' => $to,
            'summary' => $repo->summary($from, $to),
            'monthly' => $repo->monthly($from, $to),
            'top' => $repo->topProducts($from, $to, 10),
            'invoices' => $repo->invoices($from, $to, 50),
        ]);
    }
}
```

- [ ] **Step 6: Implement sales.php view**

Create `app/Views/sales.php`. Uses partials that are finalized in Task 5; write self-contained markup here (no partial dependency yet) so the page renders now:
```php
<?php
/** @var array $summary @var array $monthly @var array $top @var array $invoices */
$fmt = fn($n) => number_format((float)$n, 0) . ' ر.س';
?>
<h1>تقرير المبيعات</h1>

<form method="get" class="filters">
    <label>من: <input type="date" name="from" value="<?= htmlspecialchars($from ?? '') ?>"></label>
    <label>إلى: <input type="date" name="to" value="<?= htmlspecialchars($to ?? '') ?>"></label>
    <button type="submit">تطبيق</button>
    <a class="export" href="/export?report=sales&format=csv&from=<?= urlencode($from ?? '') ?>&to=<?= urlencode($to ?? '') ?>">تصدير CSV</a>
    <a class="export" href="/export?report=sales&format=xls&from=<?= urlencode($from ?? '') ?>&to=<?= urlencode($to ?? '') ?>">تصدير Excel</a>
    <button type="button" onclick="window.print()">طباعة / PDF</button>
</form>

<section class="kpis">
    <div class="kpi"><span class="kpi-label">إجمالي المبيعات</span><span class="kpi-value"><?= $fmt($summary['total']) ?></span></div>
    <div class="kpi"><span class="kpi-label">عدد الفواتير</span><span class="kpi-value"><?= (int)$summary['count'] ?></span></div>
    <div class="kpi"><span class="kpi-label">متوسط الفاتورة</span><span class="kpi-value"><?= $fmt($summary['avg']) ?></span></div>
</section>

<section class="card">
    <h2>المبيعات الشهرية</h2>
    <canvas id="salesChart" data-values='<?= json_encode(array_map(fn($m)=>["label"=>$m["ym"],"value"=>$m["total"]], $monthly), JSON_UNESCAPED_UNICODE) ?>'></canvas>
</section>

<section class="card">
    <h2>أفضل المنتجات</h2>
    <table>
        <thead><tr><th>المنتج</th><th>الكمية</th><th>الإيراد</th></tr></thead>
        <tbody>
        <?php foreach ($top as $t): ?>
            <tr><td><?= htmlspecialchars($t['name']) ?></td><td><?= (int)$t['qty'] ?></td><td><?= $fmt($t['revenue']) ?></td></tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</section>

<section class="card">
    <h2>آخر الفواتير</h2>
    <table>
        <thead><tr><th>رقم الفاتورة</th><th>العميل</th><th>التاريخ</th><th>الإجمالي</th></tr></thead>
        <tbody>
        <?php foreach ($invoices as $inv): ?>
            <tr><td><?= htmlspecialchars($inv['invoice_no']) ?></td><td><?= htmlspecialchars($inv['customer_name']) ?></td><td><?= htmlspecialchars($inv['invoice_date']) ?></td><td><?= $fmt($inv['total']) ?></td></tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</section>
```

- [ ] **Step 7: Smoke-test the page over HTTP**

Start server (background):
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" -S 127.0.0.1:8010 -t public
```
Run:
```bash
curl -s "http://127.0.0.1:8010/sales" | grep -c "تقرير المبيعات"
```
Expected: `1` or more (page renders). Stop the server.

- [ ] **Step 8: Commit and push**

```bash
git add -A && git commit -m "feat(sales): repository, controller, view" && git push
```

---

### Task 5: Design system (layout, CSS, JS, partials) + Inventory report

**Files:**
- Modify: `app/Views/layout.php` (full RTL shell: sidebar, header, assets)
- Create: `assets/css/app.css`
- Create: `assets/js/app.js`
- Create: `app/Models/InventoryRepository.php`
- Create: `app/Controllers/InventoryController.php`
- Create: `app/Views/inventory.php`
- Modify: `public/index.php` (add a static asset passthrough for `php -S`)
- Test: `tests/unit/inventory_repo_test.php`

**Interfaces:**
- Consumes: `Database::connection()`, `Request::get()`.
- Produces:
  - `InventoryRepository::summary(?string $category): array` → `['items'=>int,'value'=>float,'low'=>int]` (value = Σ stock_qty*cost).
  - `InventoryRepository::products(?string $category): array` → product rows + computed `low` boolean (`stock_qty <= reorder_level`).
  - `InventoryRepository::valueByCategory(): array` → `['category'=>string,'value'=>float]` list.
  - `InventoryRepository::categories(): array` → distinct category strings.

- [ ] **Step 1: Write the failing test**

Create `tests/unit/inventory_repo_test.php`:
```php
<?php
require __DIR__ . '/../../app/Core/Database.php';
require __DIR__ . '/../../app/Models/InventoryRepository.php';

$repo = new InventoryRepository();
$s = $repo->summary(null);
assert($s['items'] > 0, 'items > 0');
assert($s['value'] > 0, 'stock value > 0');
assert($s['low'] >= 0, 'low count >= 0');

$prods = $repo->products(null);
assert(count($prods) === $s['items'], 'products count matches summary items');
assert(array_key_exists('low', $prods[0]), 'product row has low flag');

$byCat = $repo->valueByCategory();
assert(count($byCat) > 0, 'value by category not empty');
assert(isset($byCat[0]['category'], $byCat[0]['value']), 'row shape');

$cats = $repo->categories();
assert(count($cats) > 0, 'categories not empty');

echo "inventory_repo_test OK\n";
```

- [ ] **Step 2: Run test to verify it fails**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/inventory_repo_test.php
```
Expected: FAIL — cannot open `InventoryRepository.php`.

- [ ] **Step 3: Implement InventoryRepository.php**

Create `app/Models/InventoryRepository.php`:
```php
<?php

class InventoryRepository
{
    private PDO $db;
    public function __construct() { $this->db = Database::connection(); }

    public function summary(?string $category): array
    {
        $w = $category ? ' WHERE category = ?' : '';
        $p = $category ? [$category] : [];
        $stmt = $this->db->prepare(
            "SELECT COUNT(*) items,
                    COALESCE(SUM(stock_qty * cost),0) value,
                    COALESCE(SUM(CASE WHEN stock_qty <= reorder_level THEN 1 ELSE 0 END),0) low
             FROM products$w"
        );
        $stmt->execute($p);
        $r = $stmt->fetch();
        return ['items' => (int)$r['items'], 'value' => (float)$r['value'], 'low' => (int)$r['low']];
    }

    public function products(?string $category): array
    {
        $w = $category ? ' WHERE category = ?' : '';
        $p = $category ? [$category] : [];
        $stmt = $this->db->prepare("SELECT * FROM products$w ORDER BY category, name");
        $stmt->execute($p);
        return array_map(function ($r) {
            $r['low'] = ((int)$r['stock_qty'] <= (int)$r['reorder_level']);
            return $r;
        }, $stmt->fetchAll());
    }

    public function valueByCategory(): array
    {
        $stmt = $this->db->query(
            "SELECT category, SUM(stock_qty * cost) value FROM products GROUP BY category ORDER BY value DESC"
        );
        return array_map(fn($r) => ['category' => $r['category'], 'value' => (float)$r['value']], $stmt->fetchAll());
    }

    public function categories(): array
    {
        return array_map(fn($r) => $r['category'],
            $this->db->query('SELECT DISTINCT category FROM products ORDER BY category')->fetchAll());
    }
}
```

- [ ] **Step 4: Run the repo test — verify it passes**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/inventory_repo_test.php
```
Expected: `inventory_repo_test OK`.

- [ ] **Step 5: Implement InventoryController.php**

Create `app/Controllers/InventoryController.php`:
```php
<?php

class InventoryController extends Controller
{
    public function index(): void
    {
        $repo = new InventoryRepository();
        $category = Request::get('category');
        $this->render('inventory', [
            'title' => 'تقرير المخزون',
            'active' => 'inventory',
            'category' => $category,
            'categories' => $repo->categories(),
            'summary' => $repo->summary($category),
            'products' => $repo->products($category),
            'byCategory' => $repo->valueByCategory(),
        ]);
    }
}
```

- [ ] **Step 6: Build the full RTL layout**

Replace `app/Views/layout.php`:
```php
<?php /** @var string $__view */ $active = $active ?? ''; ?>
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><?= htmlspecialchars($title ?? 'تقارير ERP') ?></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/assets/css/app.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>
</head>
<body>
<aside class="sidebar">
    <div class="brand">تقارير ERP</div>
    <nav>
        <a href="/" class="<?= $active==='dashboard'?'on':'' ?>">لوحة التحكم</a>
        <a href="/sales" class="<?= $active==='sales'?'on':'' ?>">المبيعات</a>
        <a href="/inventory" class="<?= $active==='inventory'?'on':'' ?>">المخزون</a>
        <a href="/finance" class="<?= $active==='finance'?'on':'' ?>">المالية</a>
    </nav>
    <div class="sidebar-foot">SmartERP • 2026</div>
</aside>
<div class="main">
    <header class="topbar"><h1 class="topbar-title"><?= htmlspecialchars($title ?? '') ?></h1></header>
    <main class="content">
        <?php require $__view; ?>
    </main>
</div>
<script src="/assets/js/app.js"></script>
</body>
</html>
```

- [ ] **Step 7: Write the design-system CSS**

Create `assets/css/app.css`:
```css
:root{
  --bg:#f5f7fb; --card:#fff; --ink:#1f2937; --muted:#6b7280;
  --brand:#2563eb; --brand-d:#1e40af; --line:#e5e7eb;
  --good:#16a34a; --bad:#dc2626; --warn:#d97706;
}
*{box-sizing:border-box}
body{margin:0;font-family:'Cairo',system-ui,sans-serif;background:var(--bg);color:var(--ink);display:flex;min-height:100vh}
.sidebar{width:240px;background:linear-gradient(180deg,var(--brand-d),var(--brand));color:#fff;display:flex;flex-direction:column;padding:20px 0}
.brand{font-weight:700;font-size:1.3rem;text-align:center;padding:8px 16px 20px}
.sidebar nav{display:flex;flex-direction:column;gap:4px;padding:0 12px}
.sidebar nav a{color:#dbeafe;text-decoration:none;padding:11px 16px;border-radius:10px;font-weight:600;transition:.15s}
.sidebar nav a:hover{background:rgba(255,255,255,.12);color:#fff}
.sidebar nav a.on{background:#fff;color:var(--brand-d)}
.sidebar-foot{margin-top:auto;text-align:center;color:#bfdbfe;font-size:.8rem;padding:16px}
.main{flex:1;display:flex;flex-direction:column;min-width:0}
.topbar{background:var(--card);border-bottom:1px solid var(--line);padding:18px 28px}
.topbar-title{margin:0;font-size:1.4rem}
.content{padding:28px;max-width:1200px}
h1{font-size:1.5rem;margin:0 0 20px}
h2{font-size:1.1rem;margin:0 0 14px}
.filters{display:flex;gap:12px;align-items:center;flex-wrap:wrap;background:var(--card);padding:16px;border-radius:12px;border:1px solid var(--line);margin-bottom:22px}
.filters label{font-weight:600;font-size:.9rem}
.filters input{font-family:inherit;padding:8px;border:1px solid var(--line);border-radius:8px}
.filters button,.filters .export{background:var(--brand);color:#fff;border:0;padding:9px 16px;border-radius:8px;font-family:inherit;font-weight:600;cursor:pointer;text-decoration:none;font-size:.9rem}
.filters .export{background:#0f766e}
.filters button[type=button]{background:#475569}
.kpis{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:16px;margin-bottom:22px}
.kpi{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:20px;display:flex;flex-direction:column;gap:8px;box-shadow:0 1px 3px rgba(0,0,0,.04)}
.kpi-label{color:var(--muted);font-size:.9rem;font-weight:600}
.kpi-value{font-size:1.7rem;font-weight:700;color:var(--brand-d)}
.kpi.good .kpi-value{color:var(--good)} .kpi.bad .kpi-value{color:var(--bad)} .kpi.warn .kpi-value{color:var(--warn)}
.card{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:22px;margin-bottom:22px;box-shadow:0 1px 3px rgba(0,0,0,.04)}
table{width:100%;border-collapse:collapse}
th,td{text-align:right;padding:11px 14px;border-bottom:1px solid var(--line);font-size:.92rem}
th{color:var(--muted);font-weight:700;background:#f9fafb}
tbody tr:hover{background:#f9fafb}
tr.low td{background:#fef2f2}
.badge{display:inline-block;padding:3px 10px;border-radius:999px;font-size:.78rem;font-weight:700}
.badge.low{background:#fee2e2;color:var(--bad)} .badge.ok{background:#dcfce7;color:var(--good)}
canvas{max-height:320px}
@media (max-width:760px){body{flex-direction:column}.sidebar{width:100%;flex-direction:row;flex-wrap:wrap}.sidebar nav{flex-direction:row}}
@media print{
  .sidebar,.topbar,.filters{display:none!important}
  body{background:#fff}.content{padding:0;max-width:none}
  .card,.kpi{box-shadow:none;border:1px solid #ccc}
}
```

- [ ] **Step 8: Write app.js (chart bootstrapping)**

Create `assets/js/app.js`:
```js
document.addEventListener('DOMContentLoaded', function () {
  document.querySelectorAll('canvas[data-values]').forEach(function (cv) {
    var data;
    try { data = JSON.parse(cv.getAttribute('data-values')); } catch (e) { return; }
    var type = cv.getAttribute('data-type') || 'bar';
    var labels = data.map(function (d) { return d.label; });
    var values = data.map(function (d) { return d.value; });
    var colors = ['#2563eb','#0f766e','#d97706','#dc2626','#7c3aed','#16a34a'];
    new Chart(cv, {
      type: type,
      data: {
        labels: labels,
        datasets: (data[0] && data[0].series) ? buildSeries(data) : [{
          label: cv.getAttribute('data-label') || '',
          data: values,
          backgroundColor: type === 'line' ? 'rgba(37,99,235,.15)' : colors,
          borderColor: '#2563eb',
          borderWidth: 2, tension: .3, fill: type === 'line'
        }]
      },
      options: {
        responsive: true, plugins: { legend: { display: type !== 'bar' || !!(data[0] && data[0].series) } },
        scales: { y: { beginAtZero: true } }
      }
    });
  });
  function buildSeries(data){
    var keys = Object.keys(data[0].series);
    var palette = { income:'#16a34a', expense:'#dc2626' };
    return keys.map(function(k,i){
      return { label: k==='income'?'إيراد':(k==='expense'?'مصروف':k),
        data: data.map(function(d){return d.series[k];}),
        backgroundColor: palette[k] || ['#2563eb','#d97706'][i%2] };
    });
  }
});
```

- [ ] **Step 9: Serve static assets under `php -S`**

Modify `public/index.php` — add near the TOP (right after `declare`), so asset files are served directly and not routed:
```php
// Serve static assets that live outside public/ (css, js) for the built-in server.
$uriPath = parse_url($_SERVER['REQUEST_URI'] ?? '/', PHP_URL_PATH) ?? '/';
if (preg_match('#^/assets/(css|js)/([A-Za-z0-9_.-]+)$#', $uriPath, $m)) {
    $file = __DIR__ . '/../assets/' . $m[1] . '/' . $m[2];
    if (is_file($file)) {
        $types = ['css' => 'text/css', 'js' => 'application/javascript'];
        header('Content-Type: ' . $types[$m[1]] . '; charset=utf-8');
        readfile($file);
        return;
    }
    http_response_code(404); return;
}
```

- [ ] **Step 10: Implement inventory.php view**

Create `app/Views/inventory.php`:
```php
<?php
/** @var array $summary @var array $products @var array $byCategory @var array $categories */
$fmt = fn($n) => number_format((float)$n, 0) . ' ر.س';
?>
<form method="get" class="filters">
    <label>التصنيف:
        <select name="category">
            <option value="">الكل</option>
            <?php foreach ($categories as $c): ?>
                <option value="<?= htmlspecialchars($c) ?>" <?= ($category ?? '')===$c?'selected':'' ?>><?= htmlspecialchars($c) ?></option>
            <?php endforeach; ?>
        </select>
    </label>
    <button type="submit">تطبيق</button>
    <a class="export" href="/export?report=inventory&format=csv&category=<?= urlencode($category ?? '') ?>">تصدير CSV</a>
    <a class="export" href="/export?report=inventory&format=xls&category=<?= urlencode($category ?? '') ?>">تصدير Excel</a>
    <button type="button" onclick="window.print()">طباعة / PDF</button>
</form>

<section class="kpis">
    <div class="kpi"><span class="kpi-label">إجمالي الأصناف</span><span class="kpi-value"><?= (int)$summary['items'] ?></span></div>
    <div class="kpi"><span class="kpi-label">قيمة المخزون</span><span class="kpi-value"><?= $fmt($summary['value']) ?></span></div>
    <div class="kpi <?= $summary['low']>0?'bad':'good' ?>"><span class="kpi-label">أصناف ناقصة</span><span class="kpi-value"><?= (int)$summary['low'] ?></span></div>
</section>

<section class="card">
    <h2>قيمة المخزون حسب التصنيف</h2>
    <canvas id="invChart" data-type="bar" data-label="القيمة"
      data-values='<?= json_encode(array_map(fn($r)=>["label"=>$r["category"],"value"=>$r["value"]], $byCategory), JSON_UNESCAPED_UNICODE) ?>'></canvas>
</section>

<section class="card">
    <h2>المنتجات</h2>
    <table>
        <thead><tr><th>المنتج</th><th>التصنيف</th><th>الكمية</th><th>حد الطلب</th><th>سعر البيع</th><th>الحالة</th></tr></thead>
        <tbody>
        <?php foreach ($products as $p): ?>
            <tr class="<?= $p['low']?'low':'' ?>">
                <td><?= htmlspecialchars($p['name']) ?></td>
                <td><?= htmlspecialchars($p['category']) ?></td>
                <td><?= (int)$p['stock_qty'] ?></td>
                <td><?= (int)$p['reorder_level'] ?></td>
                <td><?= $fmt($p['price']) ?></td>
                <td><span class="badge <?= $p['low']?'low':'ok' ?>"><?= $p['low']?'ناقص':'متوفر' ?></span></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</section>
```

- [ ] **Step 11: Smoke-test both pages + asset over HTTP**

Start server (background), then:
```bash
curl -s "http://127.0.0.1:8010/inventory" | grep -c "تقرير المخزون"
curl -s -o /dev/null -w "%{http_code} %{content_type}\n" "http://127.0.0.1:8010/assets/css/app.css"
```
Expected: first ≥1; second `200 text/css; charset=utf-8`. Stop server.

- [ ] **Step 12: Commit and push**

```bash
git add -A && git commit -m "feat(ui): design system + layout + inventory report" && git push
```

---

### Task 6: Finance report + Dashboard

**Files:**
- Create: `app/Models/FinanceRepository.php`
- Create: `app/Controllers/FinanceController.php`
- Create: `app/Views/finance.php`
- Create: `app/Controllers/DashboardController.php`
- Create: `app/Views/dashboard.php`
- Test: `tests/unit/finance_repo_test.php`

**Interfaces:**
- Consumes: `Database::connection()`, `Request::get()`, `SalesRepository`, `InventoryRepository`.
- Produces:
  - `FinanceRepository::summary(?string $from, ?string $to): array` → `['income'=>float,'expense'=>float,'net'=>float]`.
  - `FinanceRepository::monthly(?string $from, ?string $to): array` → list `['ym'=>..., 'series'=>['income'=>float,'expense'=>float]]` ordered by ym.
  - `FinanceRepository::transactions(?string $from, ?string $to, int $limit=50): array` → transaction rows.

- [ ] **Step 1: Write the failing test**

Create `tests/unit/finance_repo_test.php`:
```php
<?php
require __DIR__ . '/../../app/Core/Database.php';
require __DIR__ . '/../../app/Models/FinanceRepository.php';

$repo = new FinanceRepository();
$s = $repo->summary(null, null);
assert($s['income'] > 0, 'income > 0');
assert($s['expense'] > 0, 'expense > 0');
assert(abs($s['net'] - ($s['income'] - $s['expense'])) < 0.01, 'net = income - expense');

$m = $repo->monthly(null, null);
assert(count($m) > 0, 'monthly not empty');
assert(isset($m[0]['ym'], $m[0]['series']['income'], $m[0]['series']['expense']), 'monthly row shape');

$t = $repo->transactions(null, null, 10);
assert(count($t) > 0 && count($t) <= 10, 'transactions limit');

echo "finance_repo_test OK\n";
```

- [ ] **Step 2: Run test to verify it fails**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/finance_repo_test.php
```
Expected: FAIL — cannot open `FinanceRepository.php`.

- [ ] **Step 3: Implement FinanceRepository.php**

Create `app/Models/FinanceRepository.php`:
```php
<?php

class FinanceRepository
{
    private PDO $db;
    public function __construct() { $this->db = Database::connection(); }

    private function range(?string $from, ?string $to): array
    {
        $where = [];
        $params = [];
        if ($from) { $where[] = 'txn_date >= ?'; $params[] = $from; }
        if ($to)   { $where[] = 'txn_date <= ?'; $params[] = $to; }
        return [$where ? ' WHERE ' . implode(' AND ', $where) : '', $params];
    }

    public function summary(?string $from, ?string $to): array
    {
        [$w, $p] = $this->range($from, $to);
        $stmt = $this->db->prepare(
            "SELECT
               COALESCE(SUM(CASE WHEN type='income' THEN amount END),0) income,
               COALESCE(SUM(CASE WHEN type='expense' THEN amount END),0) expense
             FROM transactions$w"
        );
        $stmt->execute($p);
        $r = $stmt->fetch();
        $income = (float)$r['income']; $expense = (float)$r['expense'];
        return ['income' => $income, 'expense' => $expense, 'net' => $income - $expense];
    }

    public function monthly(?string $from, ?string $to): array
    {
        [$w, $p] = $this->range($from, $to);
        $stmt = $this->db->prepare(
            "SELECT substr(txn_date,1,7) ym,
                    COALESCE(SUM(CASE WHEN type='income' THEN amount END),0) income,
                    COALESCE(SUM(CASE WHEN type='expense' THEN amount END),0) expense
             FROM transactions$w GROUP BY ym ORDER BY ym"
        );
        $stmt->execute($p);
        return array_map(fn($r) => [
            'ym' => $r['ym'],
            'series' => ['income' => (float)$r['income'], 'expense' => (float)$r['expense']],
        ], $stmt->fetchAll());
    }

    public function transactions(?string $from, ?string $to, int $limit = 50): array
    {
        [$w, $p] = $this->range($from, $to);
        $p[] = $limit;
        $stmt = $this->db->prepare(
            "SELECT txn_date, type, category, amount, description
             FROM transactions$w ORDER BY txn_date DESC, id DESC LIMIT ?"
        );
        $stmt->execute($p);
        return $stmt->fetchAll();
    }
}
```

- [ ] **Step 4: Run the repo test — verify it passes**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/finance_repo_test.php
```
Expected: `finance_repo_test OK`.

- [ ] **Step 5: Implement FinanceController.php**

Create `app/Controllers/FinanceController.php`:
```php
<?php

class FinanceController extends Controller
{
    public function index(): void
    {
        $repo = new FinanceRepository();
        $from = Request::get('from');
        $to = Request::get('to');
        $this->render('finance', [
            'title' => 'التقرير المالي',
            'active' => 'finance',
            'from' => $from,
            'to' => $to,
            'summary' => $repo->summary($from, $to),
            'monthly' => $repo->monthly($from, $to),
            'transactions' => $repo->transactions($from, $to, 50),
        ]);
    }
}
```

- [ ] **Step 6: Implement finance.php view**

Create `app/Views/finance.php`:
```php
<?php
/** @var array $summary @var array $monthly @var array $transactions */
$fmt = fn($n) => number_format((float)$n, 0) . ' ر.س';
?>
<form method="get" class="filters">
    <label>من: <input type="date" name="from" value="<?= htmlspecialchars($from ?? '') ?>"></label>
    <label>إلى: <input type="date" name="to" value="<?= htmlspecialchars($to ?? '') ?>"></label>
    <button type="submit">تطبيق</button>
    <a class="export" href="/export?report=finance&format=csv&from=<?= urlencode($from ?? '') ?>&to=<?= urlencode($to ?? '') ?>">تصدير CSV</a>
    <a class="export" href="/export?report=finance&format=xls&from=<?= urlencode($from ?? '') ?>&to=<?= urlencode($to ?? '') ?>">تصدير Excel</a>
    <button type="button" onclick="window.print()">طباعة / PDF</button>
</form>

<section class="kpis">
    <div class="kpi good"><span class="kpi-label">الإيرادات</span><span class="kpi-value"><?= $fmt($summary['income']) ?></span></div>
    <div class="kpi bad"><span class="kpi-label">المصروفات</span><span class="kpi-value"><?= $fmt($summary['expense']) ?></span></div>
    <div class="kpi <?= $summary['net']>=0?'good':'bad' ?>"><span class="kpi-label">صافي الربح</span><span class="kpi-value"><?= $fmt($summary['net']) ?></span></div>
</section>

<section class="card">
    <h2>الإيرادات مقابل المصروفات (شهرياً)</h2>
    <canvas id="finChart" data-type="bar"
      data-values='<?= json_encode(array_map(fn($m)=>["label"=>$m["ym"],"series"=>$m["series"]], $monthly), JSON_UNESCAPED_UNICODE) ?>'></canvas>
</section>

<section class="card">
    <h2>آخر الحركات المالية</h2>
    <table>
        <thead><tr><th>التاريخ</th><th>النوع</th><th>التصنيف</th><th>المبلغ</th><th>الوصف</th></tr></thead>
        <tbody>
        <?php foreach ($transactions as $t): ?>
            <tr>
                <td><?= htmlspecialchars($t['txn_date']) ?></td>
                <td><span class="badge <?= $t['type']==='income'?'ok':'low' ?>"><?= $t['type']==='income'?'إيراد':'مصروف' ?></span></td>
                <td><?= htmlspecialchars($t['category']) ?></td>
                <td><?= $fmt($t['amount']) ?></td>
                <td><?= htmlspecialchars($t['description']) ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</section>
```

- [ ] **Step 7: Implement DashboardController.php**

Create `app/Controllers/DashboardController.php`:
```php
<?php

class DashboardController extends Controller
{
    public function index(): void
    {
        $sales = new SalesRepository();
        $inv = new InventoryRepository();
        $fin = new FinanceRepository();
        $this->render('dashboard', [
            'title' => 'لوحة التحكم',
            'active' => 'dashboard',
            'salesSummary' => $sales->summary(null, null),
            'salesMonthly' => $sales->monthly(null, null),
            'invSummary' => $inv->summary(null),
            'finSummary' => $fin->summary(null, null),
            'finMonthly' => $fin->monthly(null, null),
            'recentInvoices' => $sales->invoices(null, null, 8),
        ]);
    }
}
```

- [ ] **Step 8: Implement dashboard.php view**

Create `app/Views/dashboard.php`:
```php
<?php
/** @var array $salesSummary @var array $invSummary @var array $finSummary @var array $salesMonthly @var array $finMonthly @var array $recentInvoices */
$fmt = fn($n) => number_format((float)$n, 0) . ' ر.س';
?>
<section class="kpis">
    <div class="kpi"><span class="kpi-label">إجمالي المبيعات</span><span class="kpi-value"><?= $fmt($salesSummary['total']) ?></span></div>
    <div class="kpi <?= $finSummary['net']>=0?'good':'bad' ?>"><span class="kpi-label">صافي الربح</span><span class="kpi-value"><?= $fmt($finSummary['net']) ?></span></div>
    <div class="kpi"><span class="kpi-label">قيمة المخزون</span><span class="kpi-value"><?= $fmt($invSummary['value']) ?></span></div>
    <div class="kpi <?= $invSummary['low']>0?'warn':'good' ?>"><span class="kpi-label">أصناف ناقصة</span><span class="kpi-value"><?= (int)$invSummary['low'] ?></span></div>
</section>

<section class="card">
    <h2>المبيعات الشهرية</h2>
    <canvas id="dashSales" data-type="line" data-label="المبيعات"
      data-values='<?= json_encode(array_map(fn($m)=>["label"=>$m["ym"],"value"=>$m["total"]], $salesMonthly), JSON_UNESCAPED_UNICODE) ?>'></canvas>
</section>

<section class="card">
    <h2>الإيرادات مقابل المصروفات</h2>
    <canvas id="dashFin" data-type="bar"
      data-values='<?= json_encode(array_map(fn($m)=>["label"=>$m["ym"],"series"=>$m["series"]], $finMonthly), JSON_UNESCAPED_UNICODE) ?>'></canvas>
</section>

<section class="card">
    <h2>آخر الفواتير</h2>
    <table>
        <thead><tr><th>رقم الفاتورة</th><th>العميل</th><th>التاريخ</th><th>الإجمالي</th></tr></thead>
        <tbody>
        <?php foreach ($recentInvoices as $inv): ?>
            <tr><td><?= htmlspecialchars($inv['invoice_no']) ?></td><td><?= htmlspecialchars($inv['customer_name']) ?></td><td><?= htmlspecialchars($inv['invoice_date']) ?></td><td><?= $fmt($inv['total']) ?></td></tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</section>
```

- [ ] **Step 9: Smoke-test finance + dashboard over HTTP**

Start server (background), then:
```bash
curl -s "http://127.0.0.1:8010/finance" | grep -c "التقرير المالي"
curl -s "http://127.0.0.1:8010/" | grep -c "لوحة التحكم"
```
Expected: both ≥1. Stop server.

- [ ] **Step 10: Commit and push**

```bash
git add -A && git commit -m "feat(finance,dashboard): repositories, controllers, views" && git push
```

---

### Task 7: Export (CSV + Excel)

**Files:**
- Create: `app/Controllers/ExportController.php`
- Test: `tests/unit/export_test.php`

**Interfaces:**
- Consumes: `SalesRepository`, `InventoryRepository`, `FinanceRepository`, `Request::get()`.
- Produces: `ExportController::index()` reading `report` (sales|inventory|finance) and `format` (csv|xls), streaming a downloadable file with correct headers. Rows mirror each report's main table.

- [ ] **Step 1: Write the failing test**

Create `tests/unit/export_test.php` (invokes controller logic directly by capturing output):
```php
<?php
require __DIR__ . '/../../app/Core/Database.php';
require __DIR__ . '/../../app/Core/Controller.php';
require __DIR__ . '/../../app/Core/Request.php';
require __DIR__ . '/../../app/Models/SalesRepository.php';
require __DIR__ . '/../../app/Models/InventoryRepository.php';
require __DIR__ . '/../../app/Models/FinanceRepository.php';
require __DIR__ . '/../../app/Controllers/ExportController.php';

$_GET = ['report' => 'sales', 'format' => 'csv'];
ob_start();
(new ExportController())->index();
$out = ob_get_clean();
assert(str_contains($out, 'invoice_no') || str_contains($out, 'رقم الفاتورة'), 'csv has header');
assert(substr_count($out, "\n") > 5, 'csv has multiple rows');

$_GET = ['report' => 'inventory', 'format' => 'xls'];
ob_start();
(new ExportController())->index();
$out = ob_get_clean();
assert(str_contains($out, '<table'), 'xls is html table');

echo "export_test OK\n";
```

- [ ] **Step 2: Run test to verify it fails**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/export_test.php
```
Expected: FAIL — cannot open `ExportController.php`.

- [ ] **Step 3: Implement ExportController.php**

Create `app/Controllers/ExportController.php`:
```php
<?php

class ExportController extends Controller
{
    public function index(): void
    {
        $report = Request::get('report', 'sales');
        $format = Request::get('format', 'csv');
        $from = Request::get('from');
        $to = Request::get('to');
        $category = Request::get('category');

        [$headers, $rows] = $this->dataset($report, $from, $to, $category);
        $filename = $report . '-report.' . ($format === 'xls' ? 'xls' : 'csv');

        if ($format === 'xls') {
            $this->xls($headers, $rows, $filename);
        } else {
            $this->csv($headers, $rows, $filename);
        }
    }

    private function dataset(string $report, ?string $from, ?string $to, ?string $category): array
    {
        if ($report === 'inventory') {
            $rows = array_map(fn($p) => [
                $p['name'], $p['category'], $p['stock_qty'], $p['reorder_level'],
                number_format((float)$p['price'], 0), $p['low'] ? 'ناقص' : 'متوفر',
            ], (new InventoryRepository())->products($category));
            return [['المنتج','التصنيف','الكمية','حد الطلب','السعر','الحالة'], $rows];
        }
        if ($report === 'finance') {
            $rows = array_map(fn($t) => [
                $t['txn_date'], $t['type']==='income'?'إيراد':'مصروف', $t['category'],
                number_format((float)$t['amount'], 0), $t['description'],
            ], (new FinanceRepository())->transactions($from, $to, 1000));
            return [['التاريخ','النوع','التصنيف','المبلغ','الوصف'], $rows];
        }
        // default: sales
        $rows = array_map(fn($i) => [
            $i['invoice_no'], $i['customer_name'], $i['invoice_date'],
            number_format((float)$i['total'], 0),
        ], (new SalesRepository())->invoices($from, $to, 1000));
        return [['رقم الفاتورة','العميل','التاريخ','الإجمالي'], $rows];
    }

    private function csv(array $headers, array $rows, string $filename): void
    {
        header('Content-Type: text/csv; charset=utf-8');
        header('Content-Disposition: attachment; filename="' . $filename . '"');
        echo "\xEF\xBB\xBF"; // UTF-8 BOM so Excel reads Arabic
        $out = fopen('php://output', 'w');
        fputcsv($out, $headers);
        foreach ($rows as $r) { fputcsv($out, $r); }
        fclose($out);
    }

    private function xls(array $headers, array $rows, string $filename): void
    {
        header('Content-Type: application/vnd.ms-excel; charset=utf-8');
        header('Content-Disposition: attachment; filename="' . $filename . '"');
        echo "\xEF\xBB\xBF<meta charset='utf-8'><table border='1'>";
        echo '<tr>';
        foreach ($headers as $h) { echo '<th>' . htmlspecialchars($h) . '</th>'; }
        echo '</tr>';
        foreach ($rows as $r) {
            echo '<tr>';
            foreach ($r as $c) { echo '<td>' . htmlspecialchars((string)$c) . '</td>'; }
            echo '</tr>';
        }
        echo '</table>';
    }
}
```

- [ ] **Step 4: Run the export test — verify it passes**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/unit/export_test.php
```
Expected: `export_test OK`.

- [ ] **Step 5: Smoke-test export over HTTP**

Start server (background), then:
```bash
curl -s -D - -o /dev/null "http://127.0.0.1:8010/export?report=sales&format=csv" | grep -i "content-disposition"
```
Expected: a `Content-Disposition: attachment; filename="sales-report.csv"` header. Stop server.

- [ ] **Step 6: Commit and push**

```bash
git add -A && git commit -m "feat(export): CSV + Excel export for all reports" && git push
```

---

### Task 8: All-tests runner + Playwright E2E + screenshots

**Files:**
- Create: `tests/run_unit.php` (runs every `tests/unit/*_test.php`)
- Create: `tests/e2e/README.md` (how the Playwright pass is driven via MCP)
- Create (by the E2E pass): `tests/screenshots/*.png`

**Interfaces:**
- Consumes: the running server at `http://127.0.0.1:8010`.
- Produces: a green unit run + screenshots of all four pages for the project report.

- [ ] **Step 1: Write the unit runner**

Create `tests/run_unit.php`:
```php
<?php
$files = glob(__DIR__ . '/unit/*_test.php');
$fail = 0;
foreach ($files as $f) {
    echo "== " . basename($f) . " ==\n";
    $php = 'C:/wamp64/bin/php/php8.3.28/php.exe';
    passthru('"' . $php . '" "' . $f . '"', $code);
    if ($code !== 0) { $fail++; echo "FAILED\n"; }
}
echo $fail === 0 ? "\nALL UNIT TESTS PASSED\n" : "\n$fail SUITE(S) FAILED\n";
exit($fail === 0 ? 0 : 1);
```

- [ ] **Step 2: Run all unit tests**

Run:
```bash
"C:/wamp64/bin/php/php8.3.28/php.exe" tests/run_unit.php
```
Expected: `ALL UNIT TESTS PASSED`.

- [ ] **Step 3: Document the E2E procedure**

Create `tests/e2e/README.md`:
```markdown
# اختبار E2E عبر Playwright MCP

المتطلب: تشغيل الخادم أولاً:
```
"C:/wamp64/bin/php/php8.3.28/php.exe" -S 127.0.0.1:8010 -t public
```

الخطوات (تُنفَّذ عبر أدوات Playwright MCP):
1. الانتقال إلى `/` والتحقق من ظهور "لوحة التحكم" و 4 بطاقات KPI و canvas للرسوم.
2. الانتقال إلى `/sales`، تطبيق فلتر تاريخ، التحقق من تغيّر جدول الفواتير.
3. الانتقال إلى `/inventory`، اختيار تصنيف، التحقق من تمييز الأصناف الناقصة (شارة "ناقص").
4. الانتقال إلى `/finance`، التحقق من KPIs صافي الربح والرسم.
5. التقاط لقطة شاشة كاملة لكل صفحة وحفظها في `tests/screenshots/`.
```

- [ ] **Step 4: Run the Playwright E2E pass (driven by Claude via MCP)**

Start server (background). Then, using Playwright MCP tools, for each of `/`, `/sales`, `/inventory`, `/finance`:
- `browser_navigate` to `http://127.0.0.1:8010<path>`
- `browser_snapshot` and assert key Arabic headings/KPIs/canvas are present
- On `/sales`: fill `from`/`to`, submit, snapshot again and confirm the invoice table changed
- On `/inventory`: select a category, submit, confirm "ناقص" badge appears where stock ≤ reorder
- `browser_take_screenshot` (full page) → save to `tests/screenshots/<page>.png`

Expected: all four pages render without console errors; four screenshots saved. Stop server.

- [ ] **Step 5: Commit and push**

```bash
git add -A && git commit -m "test: unit runner + Playwright E2E procedure + screenshots" && git push
```

---

### Task 9: Project contents report (final deliverable)

**Files:**
- Create: `docs/PROJECT_REPORT.md`

**Interfaces:**
- Consumes: everything built; the four screenshots from Task 8; the GitHub repo URL from Task 1.
- Produces: the final report the user asked for (requirement #4).

- [ ] **Step 1: Write PROJECT_REPORT.md**

Create `docs/PROJECT_REPORT.md` with these sections (fill real values, not placeholders):
```markdown
# تقرير محتويات المشروع — مولد تقارير ERP

## نظرة عامة
مولد تقارير احترافي بـ PHP 8.3 + SQLite، واجهات عربية RTL، يضم لوحة تحكم و٣ تقارير: المبيعات، المخزون، المالية.

## رابط المستودع
<GitHub repo URL from `gh repo view --json url -q .url`>

## البنية والمكوّنات
- **public/index.php** — Front Controller + توجيه + خدمة الأصول الثابتة.
- **app/Core** — Router، Database (PDO/SQLite)، Controller، Request.
- **app/Controllers** — Dashboard, Sales, Inventory, Finance, Export.
- **app/Models** — SalesRepository, InventoryRepository, FinanceRepository.
- **app/Views** — layout RTL + صفحات التقارير.
- **database** — schema.sql + seed.php (بيانات ١٢ شهر).
- **assets** — نظام تصميم CSS + Chart.js bootstrap.
- **tests** — وحدات (PHP) + E2E (Playwright).

## كيفية التشغيل
(انسخ من README)

## التقارير والميزات
جدول التقارير والـ KPIs والفلاتر (من الـ spec).

## نتائج الاختبار
- **اختبارات الوحدة:** الصق مخرجات `tests/run_unit.php` (ALL UNIT TESTS PASSED).
- **اختبار E2E (Playwright):** ملخص الخطوات + النتائج.

## لقطات الواجهات
![لوحة التحكم](../tests/screenshots/dashboard.png)
![المبيعات](../tests/screenshots/sales.png)
![المخزون](../tests/screenshots/inventory.png)
![المالية](../tests/screenshots/finance.png)

## سجل الرفع إلى GitHub (أول بأول)
الصق `git log --oneline` لإظهار الـ commits المرحلية.
```

- [ ] **Step 2: Populate real values**

Fill the repo URL (`gh repo view --json url -q .url`), the unit-test output, and the `git log --oneline` list into the report.

- [ ] **Step 3: Commit and push**

```bash
git add -A && git commit -m "docs: project contents report + screenshots" && git push
```

- [ ] **Step 4: Final verification**

Run:
```bash
gh repo view --web=false --json name,url,visibility
git log --oneline
```
Expected: repo exists (public), and the commit history shows the incremental milestone commits.

---

## Self-Review Notes

- **Spec coverage:** Sales/Inventory/Finance/Dashboard (Tasks 4–6), RTL design system (Task 5), CSV/Excel/print-PDF export (Task 7 + print CSS in Task 5), SQLite schema+seed (Task 3), GitHub repo creation by Claude + incremental push (Task 1 + every task commits/pushes), Playwright UI automation (Task 8), project report (Task 9). All covered.
- **Placeholder scan:** `PROJECT_REPORT.md` intentionally contains fill-in markers that Task 9 Step 2 resolves with real command output; no code steps contain placeholders.
- **Type consistency:** Repository method names/return shapes defined once in each task's Interfaces block and consumed consistently (Dashboard uses `salesSummary['total']`, `finSummary['net']`, `invSummary['value']/['low']`, matching the summary shapes). Chart `data-values` JSON shape (`{label,value}` or `{label,series:{income,expense}}`) matches `app.js` handling.
- **Seed code:** Task 3 seed.php uses clean month arithmetic (`((6 + $m) % 12) + 1`) — verified no invalid tokens.
